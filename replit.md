# Replit Adapter for AIpinho

AGENTS.md is canonical for cross-agent engineering policy.

If you are Replit Agent working on this repository:

1. Identify yourself as an engineering agent working ON AIpinho.
2. Read `./AGENTS.md`.
3. Read `./DOCUMENT_AUTHORITY.md`.
4. Use `./.agents/skills/` for reusable workflow procedures.
5. Read `./AIpinho_context_pack/docs/context/00_START_HERE.md` and
   `./AIpinho_context_pack/docs/context/09_CURRENT_FRONTIER.md` when the task
   depends on current project truth.
6. Obey the Git workflow in `./docs/engineering_agents/GIT_WORKFLOW.md`.
7. Obey validation authority in
   `./docs/engineering_agents/VALIDATION_AUTHORITY.md`.
8. Never confuse `.agents/` with `config/agents/` or
   `src/aipinho/services/agents/`.

This file is a thin Replit adapter. It must not become a second constitution.
Do not create or depend on a `.replit` runtime configuration unless a separate
future mission explicitly makes AIpinho runtime execution on Replit a supported
target.
