# Artifact Use As Context

Allowed by default:

- text;
- markdown reports;
- json reports;
- manifests;
- sanitized logs.

Denied by default:

- binary;
- unknown types;
- failed, blocked, deleted or expired artifacts.

The endpoint returns a sanitized context preview and reason code.
