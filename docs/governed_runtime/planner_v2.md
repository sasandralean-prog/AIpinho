# Planner V2

GR4 adds a planner that builds execution plans exclusively from Runtime
Contract Bundles.

The planner does not read prompts or free text. It derives stages, roles,
approvals, artifacts, dependencies, and validation requirements from contracts.
