# Sandbox Mobile View

Endpoint: `GET /api/v1/mobile/view-model/sandbox`.

The view reports status, workspace/task/artifact counters, policy summary and safe actions. Raw payloads remain hidden by default. The dashboard also includes a Sandbox Governado card linked to this endpoint.
