# Skill Pack Registry

O registry le manifests em `config/skills/packs`.

Consultas suportadas:

- por categoria;
- por agente;
- por stack de projeto;
- por pack id;
- por selecao baseada em goal, capabilities e execution mode.

Pack novo deve entrar por manifest, sem alterar o core.
