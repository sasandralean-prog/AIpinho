# Desktop Debugger and Artifacts

Agent tabs surface human messages and sanitized status. Technical inspection
remains in Details, Raw, Pipeline and Debugger 2.0.

Artifacts are accessed through the shared governed artifact gateway. Provider
credentials and local authorization tokens are not present in Launcher source,
visible URLs, reports or raw presentation.

The Codex router now exposes its existing mobile view-model service through the
agent session namespace, aligning desktop and mobile consumers without
duplicating state ownership.

