from __future__ import annotations

DESCRIPTOR = {
    "provider_id": "llama_cpp_embedding",
    "runtime_enabled": False,
    "purpose": "embedding GGUF registry for future governed vector generation",
}
