schema_version: 1
domains:
  dashboard:
    required_cards:
      - core_backend
      - realtime
      - ports
      - legacy_rag
      - maintenance
  chat:
    required_cards:
      - speaker_truth
      - context_decision
      - artifacts_feedback
  pipeline:
    required_cards:
      - task_state
      - approval
      - patch_preview
      - validation
      - skill_permission
  debugger:
    required_cards:
      - events
      - invariants
      - replay_regression
      - evals
      - vision_ocr
  config:
    required_cards:
      - connection
      - capabilities
      - rag_memory

