schema_version: 1

lifecycle:
  initial_status: active
  allowed_transitions:
    active: [superseded, expired, rejected]
    superseded: [expired]
    expired: [expired]
    rejected: [rejected]
  delete_memory_item: false
